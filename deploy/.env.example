# انسخ هذا الملف إلى .env على الخادم وعدّل القيم
# cp .env.example .env

# قاعدة بيانات PostgreSQL (محلية على نفس الخادم)
DATABASE_URL=postgresql://atlas:CHANGE_ME_STRONG_PASSWORD@127.0.0.1:5432/atlas_travel

NODE_ENV=production
PORT=8080
